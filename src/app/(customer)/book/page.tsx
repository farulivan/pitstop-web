import { Placeholder } from "@/components/shared/placeholder";

export default function BookPage() {
  return (
    <Placeholder
      title="Booking flow"
      description="Vehicle info, service type, and preferred slot — coming in the next slice."
    />
  );
}
